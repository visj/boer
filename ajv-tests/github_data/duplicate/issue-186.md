# [186] dist directory missing when using bower install

I'm using your library in my webapp (on the browser).  

I did a `bower install --save ajv` which did as expected, pulled the ajv repo into my bower_components directory.  The dist directory & ajv.min.js file are missing (in your bower.json you have the following: `"main": "dist/ajv.min.js",`).  Is this a bug or do I need to pull the library into my app a different way?
