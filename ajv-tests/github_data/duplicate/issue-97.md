# [97] Get a schema part in a file with getSchema

`getSchema` api just get the schema by name, but I want get a part schema in a file by passing property access notation.

when I run getSchema like this:

ajv.getSchem('xxx.json#/definitions/a')

I got nothing. How could it works?
