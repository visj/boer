# [2276] Cannot validate getting error no schema with key or ref "http://json-schema.org/draft-07/schema#"

Using latest AJV my code is similar to

```typescript
import taxSchema from "./api.json"
import someInput from "./test.json"
const ajv = new Ajv();
const validate = ajv.compile(taxSchema )
if(!validate(someInput as TaxRequest){
   console.error(validate.errors)
}
```

**JSON Schema**

The schema is defined in api.json and it's generated by @airtasker/spot

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    
    "BillToDetail": {
      "type": "object",
      "properties": {
        "chargedAmount": {
          "type": "number"
        },
        "currency": {
          "type": "string"
        },
        "vatIdNumber": {
          "type": "string"
        }
      },
      "required": [
        "chargedAmount",
        "currency"
      ],
      "additionalProperties": true
    },
    "TaxRequest": {
      "type": "object",
      "properties": {
       
        "orderUniqueId": {
          "type": "string"
        },
        "billToDetails": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/BillToDetail"
          }
        }
      },
      "required": [
        "orderUniqueId",
        "billToDetails"
      ],
      "additionalProperties": true
    }
  }
}
```

**Sample data**

This test.json

```json
{
  "orderUniqueId": "9123456789",
  "billToDetails": [
    {
      "vatIdNumber": "",
      "chargedAmount": 2500,
      "currency": "GBP"      
    }
  ]
}
```

Running the above 

```
Error: no schema with key or ref "http://json-schema.org/draft-07/schema#"
```

**What results did you expect?**

I expect to be able to validate a TaxRequest, of course with the documentation I see right now I have no idea how to specify TaxRequest from the definitions when specifying the validator I want.  Doing something like getSchema("TaxRequest") returns undefined.


