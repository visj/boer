# [581] Is there a way with AJV to check if a schema is valid?

I would like to check if a JSON Schema is valid.
I dynamicaly generate JSON schema and I don't want to validate a model over a schema but the JSON schema itself.