# [639] export type as constant

it would be nice to have exported standard types as constants

```js
export const JSONSchemaType = {
  STRING: 'string',
  NUMBER: 'number',
  ...
};
```

I will know that I am using right "type" in the schema