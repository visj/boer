# [685] Option to skip "required" checks?

In my code I only need to check for required fields before creating an entry in DB. But when updating existing entry I don't need to enforce all the fields to be present.

Is there any way to skip checks for "required" properties? Couldn't find it in the docs. This is so necessary.

Thanks!