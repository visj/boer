# [243] Coercion of empty string to integer to convert to null

When coercion is enabled, and empty string fails validation if the datatype must be an integer. Is it possible to convert empty strings to null when coercing to ints, numbers, etc?
