# [879] How to combine enum and array type ?

Hello,

How could we combine `type: array` with `enum: [1,2,3]` on the same object?

Actually now, if validate this schema with a `value: [1]`, it throws an error: `should be one of values given`.

Didn't find any option in docs.

Thanks