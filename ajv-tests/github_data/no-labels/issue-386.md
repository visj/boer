# [386] Why not just required:true

I see this array of strings needed to specify required fields, but why can't we just do this?

{
...
field1: {type: string, required:true},
field2: {type: number, required:true},
...
}