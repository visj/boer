# [115] Suggested improvements to enum error message.

The error message for enum is currently: `should be equal to one of values`.

It would be nice to add the data of what the permitted values are (though I can imagine some people might not want that if the allowed values are large strings). Though maybe that could be an option?

Could it be changed to: `should be equal to one of the permitted values` or something similar?
