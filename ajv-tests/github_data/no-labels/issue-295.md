# [295] exclusive properties

@epoberezkin Excuse me for question in issue, but could you point how to implement `exclusive properties` in JSON schema, i.e. we allows property "a" or "b" - but not both (we also sometimes want to require "a" or "b"). 
