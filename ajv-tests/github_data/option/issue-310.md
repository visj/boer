# [310] Option to fail schema compilation if unknown format is used

The spec seems to imply that unknown formats should pass validation. It makes it possible to simply mistype the format without any warning.
