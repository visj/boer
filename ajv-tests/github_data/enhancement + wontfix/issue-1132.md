# [1132] Support Schema and Data Files in YAML

It's becoming increasingly common to use YAML to define/describe JSON Schema documents and payloads, with Gatsby's markdown frontmatter being a good example.

It would be really nice if ajv supported loading schema and data files with a YAML flag to allow validation of these use-cases too.