# [1706] Log warnings despite strictSchema: false

https://ajv.js.org/options.html#strict-mode-options says `strictSchema: 'log'` displays log warnings and `strictSchema: false` does not. But even though I am using `strictSchema: false`, I still get log warnings.