# [1311] TypeScript interface for Schema

Currently, ajv declares schema as plain `object`. I believe it would be worth having a fully typed interface, so one can declare schemas in TypeScript and validate them build time. It would also help when writing schemas in the IDE.

If there is an interest in this, I could fire up a PR...