# [1698] Plans for Draft 2019-09 and 2020-12

Hi,

I'm just writing to get a little bit of insight into AJV's plans for updating the default schema specification (currently to `draft-07`) and what the key indicators are for the AJV project to consider updating the default specification to either `draft-2019-09` or `draft-2020-12`. 

I'm actually not too familiar with how JSON schema drafts work these days. Would the AJV project be waiting on some finalized specification for `draft-8` or are JSON schema drafts rolling out on the current `year-month` scheme for future publishing of the specification?

I guess my question is, if JSON-schema is rolling out on `year-month` specs from this point forward, would this mean that AJV will continue on the `require("ajv/dist/2019")` approach to retro fit updates on the spec over `draft-7`, or is AJV just waiting on a possible `draft-8` to be finalized before moving over to that as a default?

I actually have some downstream projects that would like to rely on some of the features of the `2019-09` draft, but unsure if it's worth taking the `2019` functionality in it's current form, or waiting until some of these newer features roll into the default specification for AJV.

Any insights you can provide would be very helpful.
Many Thanks
S


